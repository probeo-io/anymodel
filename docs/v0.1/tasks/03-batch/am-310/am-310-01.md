# AM-310-01: Implement BatchStore class

Story: [AM-310](../../stories/03-batch/am-310.md)
Status: todo

---

## Description

Disk-based batch state management.

## Acceptance Criteria

- [ ] createBatch(id) creates directory structure
- [ ] writeMeta(id, meta) writes/updates meta.json
- [ ] readMeta(id) reads and parses meta.json
- [ ] appendResult(id, result) appends JSON line to results.jsonl
- [ ] readResults(id) reads and parses all lines from results.jsonl
- [ ] writeProvider(id, providerData) writes provider.json
- [ ] readProvider(id) reads provider.json
- [ ] exists(id) checks if batch directory exists
